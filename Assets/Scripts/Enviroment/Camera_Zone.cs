using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Camera_Zone : MonoBehaviour
{
    public float minX, maxX, minY, maxY;
    public bool isMaxX, isMinX, isMaxY, isMinY = false;
}
